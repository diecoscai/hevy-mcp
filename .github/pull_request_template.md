## Summary

<!-- 1–2 bullets describing what changed. -->

## Why

<!-- Motivation — the user-visible problem this PR addresses or the gap it closes. -->

## Test plan

- [ ] `npm run check` passes
- [ ] `npm test` passes
- [ ] Dry-run verification completed for any new or changed write tool (called it once with `HEVY_MCP_ALLOW_WRITES` unset, confirmed the `{ dry_run: true, would_send: ... }` payload)
- [ ] `CHANGELOG.md` updated under `[Unreleased]` if this is user-visible

<!-- Link related issues, PRs, or Hevy API docs if relevant. -->
